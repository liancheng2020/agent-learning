# Agent 0506: Mini RAG

主题：检索增强生成 RAG。

你要掌握：文档切片、关键词检索、来源引用、无资料时拒答。

运行：

```bash
npm run demo
npm test
npm start
```

面试能讲：

- RAG 不是让模型“记住资料”，而是在回答前检索相关上下文。
- 好的 RAG 回答要带来源，避免无依据编造。
- 检索失败时应该明确说不知道，而不是硬答。
