# 2026-05-05

## 今日主题
Agent 可观测性与最小评估。

## 今日目标
实现一个会记录 JSONL trace，并能通过 eval 用例验证工具选择和回答质量的 Agent。

## 学习安排
- 20 分钟：理解 tracing 和 eval 在 Agent 工程里的作用。
- 30 分钟：阅读 `TraceLogger`，理解 JSONL 事件格式。
- 60 分钟：阅读 `runAgent`，跟踪从输入到工具调用再到回答的完整链路。
- 40 分钟：运行 `npm test`，观察 eval 报告和 `data/eval-trace.jsonl`。
- 20 分钟：写下一个失败 case，并尝试补一条 eval。

## 打卡清单
- [x] 创建 `agent-0505` 项目
- [x] 实现 `TraceLogger`
- [x] 实现 `runEval`
- [x] 跑通 `npm run demo`
- [x] 跑通 `npm test`

## 验收标准
- [x] 能生成 JSONL trace
- [x] 能统计 eval 通过率
- [x] 能看到工具选择和工具执行事件

## 明天要做
把 trace 和 eval 结果做成一个简单 Web 页面，训练前端优势和 Agent 工程结合。
